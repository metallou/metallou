{% macro dates(dateStart, dateEnd, format = 'd/m/Y') %}
  {% if dateEnd is null %}
    <i>{{ dateStart|date(format) }}</i>
  {% else %}
    <i>{{ dateStart|date(format) }} - {{ dateEnd|date(format) }}</i>
  {% endif %}
{% endmacro dates %}